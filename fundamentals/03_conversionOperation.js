
// A bug or specific design of javascript. 

let x = "apple";
console.log(typeof x);

numX = Number(x);
console.log(typeof numX);
console.log(numX);

// Num(undefined) => NaN
// Num(null) => 0;
// Num(true) => 1;
